/**
 * function number er sathe 10 gun
 */
function tenTimes(num) {
  const result = num * 10;
  return result;
}

/**
 * function curHalf. mane jei number dimu seitake tumi ordhek kore diba
 */
function cutHalf(num) {
  const result = num / 2;
  return result;
}

const output = tenTimes(2);
console.log(`output: ${output}`);
