/*
লুপ ব্যবহার করে ৫ এর নামতা (Multiplication Table) প্রিন্ট করুন।
*/

// by using for loop
for (let i = 1; i <= 10; i++) {
  console.log(`5 x ${i} = ${5 * i}`);
}

// by using while loop
let n = 1;
while (n <= 10) {
  console.log(`5 x ${n} = ${5 * n}`);
  n++;
}
