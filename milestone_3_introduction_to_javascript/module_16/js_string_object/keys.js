const computer = {
  brand: "hp",
  price: 80000,
  processor: "intel",
  ssd: "512gb",
  monitor: "Benq",
};

/**
 * object er ki ki keys ache seita janar jonno nicher code lekhte hoi
 */
const objKeys = Object.keys(computer);
console.log(objKeys);
